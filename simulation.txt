on veut simuler les histoires de vie, à partir du fit de lifelihood.

- on sait simuler un évènmenent donné (maturité, reproduction, mort)
- on veut automatiquement simuler les évènements qui ont été fitté (maturité, reproduction, mort)
- option pour paralléliser ? https://r-dev-perf.borishejblum.science/parallelisation-du-code-r

objectif final : jeu de données d'histoire de vies simulées
- fonction style "simulation(results, newdata=NULL, events="all")"
   - events --> quel events simuler
      - par défaut, 'all", on fait tout ce qui est fitté
      - sinon un parmi c("mortality", "maturity", "reproduction")


############

poisson --> si résultat pas ok, on resimule
zer<-0
  while (zer==0) 
  {datatp<-rpois(1,tp)
  ifelse(datatp==0,zer<-0,zer<-1)
  }