# Check MCMC consistency

Check if MCMC estimates are consistent with MLE

## Usage

``` r
check_mcmc_consistency(object, threshold = 0.05)
```

## Arguments

- object:

  lifelihoodResults object

- threshold:

  Maximum relative difference (default: 5%)

## Value

TRUE if consistent, FALSE if not
