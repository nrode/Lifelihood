# Find the operating system of the user

`detect_os()` finds the operating system name

## Usage

``` r
detect_os()
```

## Value

String with the name of the operating system, either "Windows" or
"Unix-like"

## Examples

``` r
detect_os()
#>  sysname 
#> "Darwin" 
```
